int main(int num) {
    int a = (1,2,true, 3, false, 5);


    

    print(a);



    int* q = NULL;

    q = NULL;

    // int* p;
    // p = NULL;

    // int b = (a1,a2)[3];
    // *(p,p) = 45;

    // a = 50;

    int test = (a = 70);
    print(test);



    // if(a > 4,a < 6, a = 45, a == 89) {}
}
